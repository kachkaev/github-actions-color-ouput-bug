name: CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo -e "\e[31mRed Text\e[0m \e[32mGreen Text\e[0m"
